{
  "name": "@wavult/cloudflare-adapter",
  "version": "1.0.0",
  "description": "Typed Cloudflare adapter for Wavult OS — all CF API calls go through here",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
